import '../ItemListContainer/ItemListContainer.css';

const ItemListContainer = (prop) => {
    return (

        <h1 className='ItemListContainer'>{prop.greeting}</h1>
    )
}

export default ItemListContainer